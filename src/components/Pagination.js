import React, { Component } from "react";
import styled from "styled-components";

const Page = styled.div``;
export default class Pagination extends Component {
  render() {
    return (
      <Page>
        <h1>Working on Pagination</h1>
      </Page>
    );
  }
}
